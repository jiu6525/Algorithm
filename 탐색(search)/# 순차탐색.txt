# 순차탐색

# def sequential_search(n, target, array):
#     # 각 원소를 하나씩 확인
#     for i in range(n):
#         # 현재의 원소가 찾고자 하는 원소와 동일할 경우
#         if array[i] == target:
#             return i + 1 # 현재의 위치 반환 (인덱스는 0부터 시작 +=1)
        
# input_data = input().split()
# n = int(input_data[0])
# target = input_data[1]

# array = input().split()

# print(sequential_search(n, target, array))